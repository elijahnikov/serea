import type { RouterOutputs } from "@serea/api";

export default function EntriesRows({
	entries,
}: { entries: RouterOutputs["watchlist"]["getEntries"] }) {
	return (
		<div>
			<p>hello</p>
		</div>
	);
}
